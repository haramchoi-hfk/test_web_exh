### How to Run Locally

```bash
npm i # install node packages
npm run dev # hot-reload supported
```

### Export Static Pages
```bash
npm run build
```

### On Production Build
- Remove the `basePath` in `next.config.js`